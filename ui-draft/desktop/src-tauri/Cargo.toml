[package]
name = "project_initializer_ui"
version = "0.1.0"
description = "Project Initializer Desktop UI"
authors = ["Project Initializer"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
